extends ./layout

block content
  .row
    .col-md-12
      h1
        | Welcome to #{applicationName}
      p.lead
        | We will use this sample project to explore different database systems with Node.js.
    .container 
      script.
        window.canvasOptions = {
          autoClear: true,
          autoCompensate: false,
          autoPushPop: true,
          canvas: true,
          centered: true,
          desynchronized: false,
          width: null,
          height: null
        };
        /* requestAnimationFrame */
          